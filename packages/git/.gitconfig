[filter "lfs"]
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
	clean = git-lfs clean -- %f
[init]
	defaultBranch = main
[user]
	name = Axel Mrak
	email = axelmrak@gmail.com
[core]
	excludesFile = /Users/axelmrak/.gitignore_global
[alias]
	wipe = !git reset --hard && git clean -fd
